

24/10/2025

Mas facil:

en lectores: 
mvn clean compile exec:java


en client web:
mvn clean compile tomcat7:run



22/10/2025


Tarea 2

En la carpeta \lectoresPublishWS\biblioteca-lectores:
Cada vez que se hacen cambios importantes en los archivos hay que hacer:
mvn clean compile

tiene que estar corriendo el servidor PostgreSQL:
mvn exec:java

Luego en el navegador compruebo que anda el xml:
http://127.0.0.1:8081/lectoresuy?wsdl

Se cierra con Ctrl + c

Me dijo la ia, pero creo que no es necesario:
mvn package


Luego en la carpeta lectoresWebClientWS:

Cuando hay cambios (incluso para generar los wsimport):
mvn clean compile

correr el servidor tomcat:
mvn tomcat7:run


URLs:
Web Service WSDL (solo para chequear al principio): 
http://127.0.0.1:8081/lectoresuy?wsdl


Página JSP: 
http://localhost:8080/lectores-web/


Notas: 
Este ya fue:
Desde src en WebClient (solo una vez, ya no se precisa):
wsimport -keep -p publicadores http://127.0.0.1:8081/lectoresuy?wsdl <--- No va mas!!!!

Ahora es con maven. Se ejecuta al compilar todo el proyecto:
mvn clean install

O simplemente los wsimport asi:
mvn jaxws:wsimport

Pero no hace falta, al tirar mvn clean compile en webClient ya genera los archivos.







10:24 27/8/2025


Chequear los contenedores corriendo:
docker ps

Chequear logs de un container:
docker logs 7753fd35977a



Ejecuta el programa Java y que se creen las tablas en la base de datos:
En Eclipse, haz clic derecho en el archivo TestConexion.java y selecciona Run As > Java Application

Verificar los resultados

En la consola de Eclipse: Deberías ver los mensajes de éxito: "Conexión establecida con éxito" y "Tablas de la base de datos verificadas y/o creadas con éxito".

En la base de datos PostgreSQL: 
Una vez conectado, podrás ver que se han creado las tablas para usuarios, lectores, bibliotecarios, donaciones_material, libros, articulos_especiales y prestamos. 
Si todo está correcto, habrás validado la conexión y la generación del esquema.





